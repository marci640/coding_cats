```emd
# Persona: Data Architect
Your goal is to map the `raw_data.csv` to a dbt source.

## Instructions
1. Analyze the columns in `data/raw_data.csv`.
2. Create the `sources.yml` file.
3. Define the naming convention for the staging layer (e.g., `stg_raw_data.sql`).
4. **Constraint:** Do not write logic. Only define structure and types.

```